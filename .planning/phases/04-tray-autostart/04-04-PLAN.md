---
phase: 04-tray-autostart
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - install-windows.ps1
  - daemon/README-windows.md
  - daemon/tests/test_windows_no_wsl.py
  - .planning/phases/04-tray-autostart/04-HARDWARE-RECORD.md
autonomous: false
requirements: [APP-01, APP-02]
must_haves:
  truths:
    - "install-windows.ps1 creates a venv, installs requirements-windows.txt, registers autostart, and launches the tray app in one run (D-09)"
    - "The static guard proves daemon + tray + autostart sources reference no WSL paths (\\wsl$, wsl.exe, /home, /mnt) (D-10, APP-02)"
    - "README-windows.md documents the tray/autostart/install steps and removes the Phase-4 promise from 'What is NOT covered'"
    - "The manual on-hardware record captures observed behavior for SC#1-5 (no-console logon, tray status, clean Quit, wsl --shutdown, fresh WSL-never-launched session)"
  artifacts:
    - path: "install-windows.ps1"
      provides: "Turnkey bootstrap: venv -> pip install -> autostart enable -> launch tray (D-09)"
      contains: "requirements-windows.txt"
    - path: "daemon/tests/test_windows_no_wsl.py"
      provides: "Static no-WSL-paths regression guard over daemon/tray/autostart sources (D-10)"
      contains: "FORBIDDEN"
    - path: "daemon/README-windows.md"
      provides: "Documented tray + autostart + install steps"
      contains: "install-windows.ps1"
    - path: ".planning/phases/04-tray-autostart/04-HARDWARE-RECORD.md"
      provides: "Operator-recorded SC#1-5 on-hardware verification (mirrors Phase 1/2/3 D-06 split)"
      contains: "SC#"
  key_links:
    - from: "install-windows.ps1"
      to: "daemon/requirements-windows.txt"
      via: "pip install -r"
      pattern: "pip install -r"
    - from: "install-windows.ps1"
      to: "daemon/autostart_windows.py"
      via: "enable() invoked with the venv pythonw + tray script"
      pattern: "autostart"
    - from: "daemon/tests/test_windows_no_wsl.py"
      to: "daemon/tray_windows.py"
      via: "source read + regex assertion"
      pattern: "tray_windows"
---

<objective>
Vertical slice — install + WSL-independence (SC#1, SC#4, SC#5). Ship the turnkey
bootstrap `install-windows.ps1` (D-09: create venv -> pip install
requirements-windows.txt -> register autostart via 04-02 -> launch the tray app),
document the tray/autostart/install steps in README-windows.md, add the
automatable static no-WSL-paths guard over the daemon + tray + autostart sources
(D-10, APP-02), and capture the manual on-hardware verification record for SC#1-5
(D-10 hardware half, mirroring the Phase 1/2/3 D-06 unit-tests-AND-hardware-record
split).

Purpose: Make first-run a single PowerShell command that leaves a headless,
login-starting tray app (SC#1), and prove — both by a CI-surviving static guard
and by an operator hardware record — that the daemon is fully WSL-independent
(APP-02 / SC#4-5).
Output: `install-windows.ps1`, extended `README-windows.md`, the no-WSL guard
test, and `04-HARDWARE-RECORD.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tray-autostart/04-CONTEXT.md
@.planning/phases/04-tray-autostart/04-RESEARCH.md
@.planning/phases/04-tray-autostart/04-PATTERNS.md
@daemon/requirements-windows.txt
@daemon/README-windows.md

<interfaces>
<!-- From 04-02 daemon/autostart_windows.py -->
enable(tray_script=None) -> None ; disable() -> None ; is_enabled() -> bool   (HKCU\Run, pythonw.exe derived from sys.executable)

<!-- From 04-03 daemon/tray_windows.py -->
main()  # tray entry: `python -m daemon.tray_windows` (or pythonw.exe daemon/tray_windows.py) launches the tray app

<!-- Static guard contract (RESEARCH "Code Examples" → "Static no-WSL-paths guard"; daemon confirmed clean today) -->
FORBIDDEN = [r"\\wsl\$", r"wsl\.exe", r"/home/", r"/mnt/"]
Assert none match in: daemon/claude_usage_daemon_windows.py, daemon/tray_windows.py, daemon/autostart_windows.py.

<!-- install-windows.ps1 sequence (D-09, RESEARCH structure + Anti-Pattern "derive path at install time") -->
1. python -m venv .venv   2. .venv\Scripts\python -m pip install -r daemon\requirements-windows.txt
3. invoke autostart enable with the venv pythonw.exe + the tray entry script (derive paths at install time, never hard-code)
4. launch the tray app (.venv\Scripts\pythonw.exe daemon\tray_windows.py)
Reuse the [HH:MM:SS] log shape for echoed progress lines.

<!-- README-windows.md "What is NOT covered here" (L117-121) currently promises Phase 4 — move tray/autostart/install OUT of NOT-covered into documented steps; keep PyInstaller (v2) + resolved MAC-cache notes accurate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static no-WSL-paths guard (D-10, APP-02)</name>
  <files>daemon/tests/test_windows_no_wsl.py</files>
  <read_first>
    - .planning/phases/04-tray-autostart/04-RESEARCH.md ("Code Examples" → "Static no-WSL-paths guard (D-10)"; "Project Constraints" — strictly additive, no WSL paths)
    - .planning/phases/04-tray-autostart/04-PATTERNS.md ("daemon/tests/test_windows_no_wsl.py" section — Path-based read, no mocks; extend to tray + autostart)
    - daemon/tests/test_windows_token.py (L1-16 header/docstring/Path convention)
    - daemon/tray_windows.py and daemon/autostart_windows.py (the new sources to also guard)
  </read_first>
  <action>
    Create daemon/tests/test_windows_no_wsl.py with the standard header + "Run: python -m pytest daemon/tests/test_windows_no_wsl.py -x -q" docstring. Define FORBIDDEN = [r"\\wsl\$", r"wsl\.exe", r"/home/", r"/mnt/"]. Write a parametrized (or looped) test asserting that for each of daemon/claude_usage_daemon_windows.py, daemon/tray_windows.py, daemon/autostart_windows.py, reading the file text and re.search of each FORBIDDEN pattern returns None — failing with a message naming the leaked pattern + file. Use Path-based reads (no mocks). This is a regression lock that survives CI without hardware (APP-02 static half).
  </action>
  <verify>
    <automated>python -m pytest daemon/tests/test_windows_no_wsl.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - daemon/tests/test_windows_no_wsl.py contains FORBIDDEN with the four patterns and references all three source files (grep -E "tray_windows|autostart_windows|claude_usage_daemon_windows" returns 3 matches)
    - `python -m pytest daemon/tests/test_windows_no_wsl.py -x -q` exits 0 (sources are clean)
    - The guard actually fails if a WSL path is introduced: temporarily proven by the executor (revert) OR by an inline self-check comment — the assertion message names the offending pattern + file
  </acceptance_criteria>
  <done>The static guard asserts daemon + tray + autostart sources reference no WSL paths and passes today; it is the CI-surviving APP-02 regression lock.</done>
</task>

<task type="auto">
  <name>Task 2: install-windows.ps1 bootstrap + README-windows.md docs (D-09)</name>
  <files>install-windows.ps1, daemon/README-windows.md</files>
  <read_first>
    - .planning/phases/04-tray-autostart/04-RESEARCH.md (D-09 sequence; "Recommended Project Structure"; Anti-Pattern "derive pythonw.exe from the venv at install time, don't hard-code"; Pitfall 3 pythonw not python)
    - .planning/phases/04-tray-autostart/04-PATTERNS.md ("install-windows.ps1" + "daemon/README-windows.md" sections)
    - daemon/README-windows.md (FULL — find the scope line L4-5 and "What is NOT covered here" L117-121 to update)
    - daemon/autostart_windows.py + daemon/tray_windows.py (the enable() signature + tray entry path the installer wires)
    - daemon/claude-usage-daemon.sh (spirit-only analog — the macOS/Linux bootstrap shape; do NOT copy, different OS)
  </read_first>
  <action>
    Create install-windows.ps1 at the repo root implementing the D-09 turnkey sequence: (1) create a venv with `python -m venv .venv`; (2) `& .venv\Scripts\python.exe -m pip install -r daemon\requirements-windows.txt`; (3) register autostart by invoking the autostart helper with the just-created venv's pythonw.exe + the tray entry (e.g. `& .venv\Scripts\python.exe -c "import daemon.autostart_windows as a; a.enable()"` run with cwd at repo root so sys.executable resolves to the venv — derive all paths at install time, NEVER hard-code an absolute path that breaks on repo move, per the Anti-Pattern + CLAUDE.md repoint lesson); (4) launch the tray app headless: `& .venv\Scripts\pythonw.exe daemon\tray_windows.py`. Echo `[HH:MM:SS]`-style progress lines. Note the execution-policy requirement (the user runs it via `powershell -ExecutionPolicy Bypass -File install-windows.ps1` or sets RemoteSigned) and do NOT download any remote/untrusted code in the script (security: bootstrap installs only the pinned requirements file). Then extend daemon/README-windows.md: move the "Tray icon, login autostart, install-windows.ps1" bullet OUT of "What is NOT covered here" into a new documented section covering: running install-windows.ps1, what the tray icon/menu shows (connected/scanning/error, Start-at-login toggle, Quit), how to disable autostart (the menu toggle or manual `reg delete HKCU\...\Run /v Clawdmeter`), and the WSL-independence note. Keep the PyInstaller (v2) and resolved-MAC-cache (Phase 3) notes accurate. Reuse the existing README tone/structure; do not duplicate Phase 2 run steps.
  </action>
  <verify>
    <automated>grep -q "requirements-windows.txt" install-windows.ps1 && grep -qi "pythonw" install-windows.ps1 && grep -q "install-windows.ps1" daemon/README-windows.md</automated>
  </verify>
  <acceptance_criteria>
    - install-windows.ps1 exists at the repo root and contains, in order, a venv creation step, `pip install -r` against daemon\requirements-windows.txt, an autostart-enable invocation, and a `pythonw.exe` tray launch
    - install-windows.ps1 references `pythonw.exe` (headless, D-08) and does NOT hard-code an absolute drive path (grep -E "[A-Z]:\\\\Users" install-windows.ps1 returns nothing)
    - install-windows.ps1 downloads no remote code (grep -iE "Invoke-WebRequest|curl|wget|iwr|DownloadString" install-windows.ps1 returns nothing)
    - daemon/README-windows.md contains "install-windows.ps1" and a tray/autostart section, and the "What is NOT covered here" list no longer claims tray/autostart/install as future (grep -A3 "What is NOT covered" daemon/README-windows.md does not list the tray/autostart/install bullet)
    - `python -m pytest daemon/tests/ -q` still exits 0
  </acceptance_criteria>
  <done>install-windows.ps1 does venv -> pip install -> autostart enable -> headless tray launch with runtime-derived paths and no remote download; README documents tray/autostart/install and disable steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual on-hardware record — SC#1-5 (D-10 hardware half)</name>
  <files>.planning/phases/04-tray-autostart/04-HARDWARE-RECORD.md</files>
  <read_first>
    - .planning/phases/04-tray-autostart/04-VALIDATION.md ("Manual-Only Verifications" table — the exact SC#1-5 instructions)
    - .planning/phases/04-tray-autostart/04-CONTEXT.md (D-10 — the manual-record + static-guard split; SC#4/SC#5 scenarios)
    - .planning/phases/03-resilience/03-03-SUMMARY.md (the Phase 3 D-06 hardware-record precedent to mirror, if present)
  </read_first>
  <action>
    This is a human-verify checkpoint, not autonomous work. Before pausing for the operator, the executor MUST create .planning/phases/04-tray-autostart/04-HARDWARE-RECORD.md pre-seeded with one section per success criterion (SC#1 through SC#5), each with the test steps and an empty "Observed:" + "Result: PASS/FAIL" line for the operator to fill. Then present the checkpoint and wait. See <what-built> / <how-to-verify> / <resume-signal> below for the operator-facing detail.
  </action>
  <what-built>
    install-windows.ps1 (turnkey venv + autostart + tray launch), the pystray tray app (per-state icon, status header, Start-at-login toggle, Quit), the winreg HKCU\Run autostart, and the static no-WSL-paths guard. Everything automatable is green; this checkpoint records the on-hardware behavior that mocks cannot prove.
  </what-built>
  <how-to-verify>
    On the target Windows PC with the Clawdmeter powered and Claude Code installed natively, with the executor first creating .planning/phases/04-tray-autostart/04-HARDWARE-RECORD.md and recording each observation:
    1. SC#1 — Run `powershell -ExecutionPolicy Bypass -File install-windows.ps1`. Log out and back in. Confirm the daemon is running with NO terminal/console window visible. Record: tray icon present at logon? console window seen? (expect: yes / none)
    2. SC#2 — Hover/click the tray icon. Confirm the corner bubble + tooltip reflect the live state (connected once data flows; scanning while searching). Record the observed states + tooltip text.
    3. SC#3 — Right-click the tray icon -> Quit. Confirm the BLE link disconnects cleanly and the process exits (no lingering pythonw.exe). Record observed exit behavior.
    4. SC#4 — With the device connected, run `wsl --shutdown`. Confirm the Clawdmeter stays connected and NO error is logged by the daemon. Record link state + any log output.
    5. SC#5 — Reboot into a fresh Windows session and do NOT launch WSL. Confirm the device connects and shows usage data normally. Record connect time + whether usage appeared.
    Capture observed (not idealized) behavior, mirroring the Phase 1/2/3 hardware notes.
  </how-to-verify>
  <verify>
    <human-check>04-HARDWARE-RECORD.md has a filled Observed + Result (PASS/FAIL) line for each of SC#1-5; operator types "approved" or names the failing SC.</human-check>
  </verify>
  <acceptance_criteria>
    - .planning/phases/04-tray-autostart/04-HARDWARE-RECORD.md exists and has a recorded observation line for each of SC#1, SC#2, SC#3, SC#4, SC#5 (grep -c "SC#" returns >= 5)
    - Each SC entry states observed behavior and PASS/FAIL (not a placeholder)
    - If any SC failed, the record notes the failure and the gap is surfaced to the orchestrator (not silently passed)
  </acceptance_criteria>
  <done>04-HARDWARE-RECORD.md captures observed PASS/FAIL behavior for SC#1-5 on the target Windows hardware; any failure is surfaced, not silently passed.</done>
  <resume-signal>Type "approved" once the hardware record is filled for SC#1-5, or describe any SC that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-windows.ps1 -> system | A PowerShell bootstrap that creates a venv, installs deps, and registers a persistence hook |
| daemon sources -> WSL filesystem | The independence guarantee — no source may reach a WSL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | install-windows.ps1 dependency install | mitigate | Install ONLY from the in-repo pinned daemon\requirements-windows.txt; no Invoke-WebRequest / remote DownloadString of untrusted code; document `-ExecutionPolicy Bypass` for the single trusted local script |
| T-04-10 | Tampering | install-windows.ps1 autostart path | mitigate | Derive the venv pythonw.exe + tray script path at install time (sys.executable / .venv\Scripts), never hard-code an absolute path that goes stale or could be hijacked on repo move (CLAUDE.md repoint lesson) |
| T-04-11 | Information Disclosure | install/README output + tray | mitigate | Echo only progress + paths via the log shape — never the OAuth token; README tells the user to run `claude login`, never to paste a token; no token logging added (ASVS V7) |
| T-04-12 | Tampering | WSL-path leakage regression | mitigate | The static no-WSL-paths guard (Task 1) is a CI-surviving regression lock asserting daemon + tray + autostart reference no `\\wsl$`/`wsl.exe`/`/home`/`/mnt` (APP-02 / D-10) |
</threat_model>

<verification>
- `python -m pytest daemon/tests/test_windows_no_wsl.py -x -q` exits 0
- `python -m pytest daemon/tests/ -q` exits 0 (full suite)
- install-windows.ps1 references requirements-windows.txt + pythonw.exe, no remote download, no hard-coded path
- README documents tray/autostart/install; "What is NOT covered" no longer claims them
- 04-HARDWARE-RECORD.md filled for SC#1-5 with PASS/FAIL per criterion
</verification>

<success_criteria>
- One-command install (venv -> pip -> autostart -> headless tray) — SC#1
- Static guard proves WSL-independence in CI; hardware record proves SC#4-5 on-device
- README documents the tray/autostart/install + disable steps; manual record captures SC#1-5
</success_criteria>

<output>
Create `.planning/phases/04-tray-autostart/04-04-SUMMARY.md` when done
</output>
