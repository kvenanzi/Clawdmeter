---
phase: 02-core-pipeline
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - daemon/README-windows.md
  - .planning/phases/02-core-pipeline/02-WINDOWS-VERIFICATION.md
autonomous: false
requirements: [BLE-01, BLE-02, POLL-01]
must_haves:
  truths:
    - "the daemon docs explain the Windows venv + pip install -r requirements-windows.txt + run steps"
    - "an operator can follow the docs to run the daemon on native Windows hardware"
    - "the device leaves its 'waiting for data' screen and shows session + weekly percentages (SC#1, SC#3)"
    - "BleakScanner finds 'Claude Controller' and BleakClient connects with the locked kwargs (SC#2)"
    - "first-paint latency from daemon start with a warm token is under 10 seconds (SC#4)"
    - "the manual verification result is recorded in a dated note, mirroring Phase 1's HUMAN-UAT"
  artifacts:
    - path: "daemon/README-windows.md"
      provides: "Windows setup + run instructions (venv, requirements-windows.txt, native token, launch)"
      contains: "requirements-windows.txt"
    - path: ".planning/phases/02-core-pipeline/02-WINDOWS-VERIFICATION.md"
      provides: "recorded manual hardware verification of SC#1-4"
      contains: "first-paint"
  key_links:
    - from: "daemon/README-windows.md"
      to: "daemon/claude_usage_daemon_windows.py"
      via: "documented launch command"
      pattern: "claude_usage_daemon_windows.py"
---

<objective>
Document the Windows setup/run procedure and record the manual on-hardware
verification that closes Phase 2's four success criteria. Per D-10, BLE
scan/connect/write (SC#1-3) and the <10s first-paint latency (SC#4) are verified
manually on native Windows hardware and recorded — exactly how Phase 1 closed its
native-Windows criterion. No mocked-bleak integration tests are written.

Purpose: Prove BLE-01, BLE-02, and POLL-01 are satisfied end-to-end on real
hardware, and leave a reproducible setup doc so the daemon can be run again.

Output: `daemon/README-windows.md` (or a Windows section in existing daemon docs)
and a dated `02-WINDOWS-VERIFICATION.md` capturing the observed result.

This plan is `autonomous: false`: the verification task pauses for the human operator
to run the daemon on Windows hardware and report what the device showed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pipeline/02-CONTEXT.md
@.planning/phases/01-foundation/01-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Windows setup + run doc</name>
  <files>daemon/README-windows.md</files>
  <read_first>
    - daemon/requirements-windows.txt (the manifest to install — created in Plan 01)
    - daemon/claude_usage_daemon_windows.py (the script to run; its __main__ runner and the native-token candidate paths)
    - .planning/phases/02-core-pipeline/02-CONTEXT.md D-11 (document the manual venv + pip install steps in daemon docs; polished install script is Phase 4 / v2)
    - .planning/phases/01-foundation/01-HUMAN-UAT.md (Phase 1 native-Windows token verification — references %USERPROFILE%\.claude\.credentials.json)
  </read_first>
  <action>
    Create `daemon/README-windows.md` documenting the Phase 2 manual run procedure (D-11 — no polished install script, that is Phase 4). Cover: (1) prerequisites — native Windows with Claude Code installed and `claude login` completed so the token exists at `%USERPROFILE%\.claude\.credentials.json` (or LOCALAPPDATA/APPDATA fallback); (2) create + activate a venv (`python -m venv .venv`, `.venv\Scripts\Activate.ps1`); (3) `pip install -r daemon\requirements-windows.txt`; (4) run `python daemon\claude_usage_daemon_windows.py`; (5) expected console output — the `[HH:MM:SS] === Claude Usage Tracker Daemon (BLE, Windows) ===` banner, `Scanning...`, `Found:`, `Connected`, and a `Sending: {"s":...}` line. Note that the daemon polls immediately on connect (warm token → first paint within seconds) and that no manual Bluetooth pairing is needed (Phase 1 D-01: characteristics unencrypted). Keep it concise and Windows-PowerShell-flavored. Do NOT document tray/autostart (Phase 4) or packaging (v2).
  </action>
  <verify>
    <automated>grep -q "requirements-windows.txt" daemon/README-windows.md && grep -q "claude_usage_daemon_windows.py" daemon/README-windows.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/README-windows.md` exists and references `requirements-windows.txt`, the venv activation, and the `python daemon\claude_usage_daemon_windows.py` launch command.
    - It states no manual Bluetooth pairing is required (Phase 1 D-01).
    - It does NOT document tray icon, autostart, or PyInstaller packaging (out of Phase 2 scope).
    - No edits to macOS/Linux daemon or firmware files (`git status --porcelain daemon/claude_usage_daemon.py daemon/claude-usage-daemon.sh` clean).
  </acceptance_criteria>
  <done>A concise Windows setup/run doc exists covering venv, install, native token, and launch, scoped to Phase 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Manual Windows hardware verification of SC#1-4</name>
  <what-built>The complete Windows daemon (Plans 01+02): scan → WinRT connect → REQ subscribe → API poll → RX write, plus the run doc (Task 1). Now an operator runs it on real Windows hardware with the Clawdmeter powered on.</what-built>
  <action>Pause for the human operator to run the daemon on native Windows hardware and confirm SC#1-4 against the live device. No code is written here — this is a verification gate; results feed Task 3's recorded note.</action>
  <how-to-verify>
    On a native Windows machine (NOT WSL) with the Clawdmeter powered on and showing its "waiting for data" bluetooth screen, and with Claude Code installed + `claude login` done:
    1. Follow `daemon/README-windows.md`: create/activate the venv, `pip install -r daemon\requirements-windows.txt`.
    2. Note the wall-clock time, then run `python daemon\claude_usage_daemon_windows.py`.
    3. SC#2 — confirm the console shows `Scanning for 'Claude Controller'`, then `Found:` (a BLE address) and `Connected` (proves BleakScanner found it and BleakClient connected with address_type="random" / use_cached_services=False).
    4. SC#1 + SC#3 — confirm a `Sending: {"s":...,"w":...}` line is logged AND the Clawdmeter leaves its waiting screen and displays session + weekly percentages (proves the firmware parsed the RX write with no nack).
    5. SC#4 — measure the elapsed time from launching the script to the device first showing percentages; confirm it is under 10 seconds with a warm (non-expired) token.
    6. Let it run a couple of minutes to confirm the 60s poll loop continues and a device button press (REQ) triggers an immediate re-poll.
    Report: did the device leave the waiting screen, what percentages showed, and the observed first-paint latency.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed first-paint latency and what the device displayed, or describe the failure (and the console output) so gaps can be planned.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verification result</name>
  <files>.planning/phases/02-core-pipeline/02-WINDOWS-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-HUMAN-UAT.md (format to mirror: dated, per-criterion expected/result, summary counts)
    - .planning/phases/02-core-pipeline/02-CONTEXT.md <specifics> (what to capture: device left waiting screen, percentages shown, observed first-paint latency)
  </read_first>
  <action>
    Create `.planning/phases/02-core-pipeline/02-WINDOWS-VERIFICATION.md` mirroring the Phase 1 `01-HUMAN-UAT.md` structure: a dated note with one entry per Success Criterion (SC#1 device leaves waiting screen + shows percentages, SC#2 scan finds device + client connects with the locked kwargs, SC#3 RX write parsed without nack, SC#4 <10s first-paint with warm token). For each, record the expected behavior and the operator's reported result (verbatim from the Task 2 resume signal, including the observed latency and displayed percentages). Add a summary count (total/passed/issues). If the operator reported a failure, record it as a gap with enough console detail for a `--gaps` replan.
  </action>
  <verify>
    <automated>grep -q "first-paint" .planning/phases/02-core-pipeline/02-WINDOWS-VERIFICATION.md && grep -qi "waiting" .planning/phases/02-core-pipeline/02-WINDOWS-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `02-WINDOWS-VERIFICATION.md` exists, is dated, and has one recorded entry per SC#1-4 with expected + operator-reported result.
    - The observed first-paint latency from Task 2's resume signal is captured (SC#4).
    - A summary count is present; any failure is recorded as a gap with console detail.
  </acceptance_criteria>
  <done>The manual hardware verification of all four Phase 2 success criteria is recorded in a dated note mirroring Phase 1's UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Windows host | Human runs the daemon; reads percentages off the device |
| daemon → Clawdmeter (BLE) | Same unencrypted local link accepted in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | README + verification note | mitigate | Docs and the verification note must not embed the OAuth token or full credential contents — reference paths and redacted forms only (mirror Phase 1's `sk-ant-…XXXX` redaction). |
| T-02-08 | Tampering | running unverified deps on hardware | mitigate | The Plan 02 package-legitimacy gate (Task 0) precedes any hardware install; this plan's install step references the already-vetted `requirements-windows.txt`. |
</threat_model>

<verification>
- `daemon/README-windows.md` documents venv + install + native token + launch, scoped to Phase 2.
- The operator confirms on hardware: device leaves waiting screen, percentages shown, connect succeeds, first-paint <10s.
- `02-WINDOWS-VERIFICATION.md` records the dated result for SC#1-4.
- No macOS/Linux daemon or firmware files touched.
</verification>

<success_criteria>
Phase 2's four ROADMAP success criteria are demonstrated on native Windows hardware and recorded: the daemon scans + connects over WinRT, writes valid usage JSON to RX, the device leaves its waiting screen showing session + weekly percentages, and first paint lands under 10 seconds with a warm token. A reproducible Windows run doc exists.
</success_criteria>

<output>
Create `.planning/phases/02-core-pipeline/02-03-SUMMARY.md` when done.
</output>
