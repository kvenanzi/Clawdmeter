---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - daemon/claude_usage_daemon_windows.py
autonomous: true
requirements: [TOKEN-01]

must_haves:
  truths:
    - "read_token() returns the access token from the primary native-Windows path %USERPROFILE%\\.claude\\.credentials.json"
    - "read_token() falls back through %LOCALAPPDATA% then %APPDATA% candidates, first hit wins"
    - "read_token() honors CLAUDE_CREDENTIALS_PATH (and CLAUDE_CONFIG_DIR) env overrides"
    - "_extract_access_token() handles both the nested claudeAiOauth and direct accessToken shapes"
    - "read_token() returns None when no candidate file exists"
    - "Running the script prints a redacted token + expiry on success, an actionable message + exit 1 on failure — never the full token"
  artifacts:
    - path: "daemon/claude_usage_daemon_windows.py"
      provides: "Windows token reader scaffold (_extract_access_token, read_token, _windows_credential_candidates, __main__)"
      exports: ["_extract_access_token", "read_token", "_windows_credential_candidates"]
      contains: "def read_token("
      min_lines: 60
  key_links:
    - from: "daemon/claude_usage_daemon_windows.py:read_token"
      to: "_extract_access_token"
      via: "calls on each candidate file's text"
      pattern: "_extract_access_token\\(.*read_text"
    - from: "daemon/claude_usage_daemon_windows.py:__main__"
      to: "read_token + redacted print"
      via: "token[-4:] redaction, never full token"
      pattern: "sk-ant-…"
---

<objective>
Implement `daemon/claude_usage_daemon_windows.py` so the RED test suite from plan 01 turns GREEN, then make the `__main__` print a redacted token + expiry. This is the thinnest end-to-end implementation that lets a real user read a Windows-local token and see a redacted confirmation — the second half of the phase's single vertical slice.

Purpose: Satisfy TOKEN-01 and Success Criteria #2 and #3. Deliver the minimal scaffold (D-05) — no BLE, no API, no logging framework, no main loop.
Output: A standalone Python file beside the macOS daemon: `_extract_access_token()` (verbatim copy, D-08), `read_token()` (file-search, first-hit-wins, env overrides per D-02/D-03), a minimal expiry helper, and a `__main__` that prints `Token OK (sk-ant-…<last4>), expires <date>` on success or an actionable message + `sys.exit(1)` on failure (D-06/D-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Source of truth for the verbatim copy (D-08): daemon/claude_usage_daemon.py §57-86 -->
<!--   def _extract_access_token(blob: str) -> str | None  — copy body byte-for-byte -->
<!-- The test suite (daemon/tests/test_windows_token.py, plan 01) imports exactly: -->
<!--   from daemon.claude_usage_daemon_windows import _extract_access_token, read_token, _windows_credential_candidates -->
<!-- read_token() must call _extract_access_token on each candidate's read_text(); -->
<!-- _windows_credential_candidates() must be a module-level function (monkeypatchable) -->
<!--   so the fallback tests can control which candidate paths exist. -->
</interfaces>
</context>

<feature>
  <name>Windows-local OAuth token reader (TOKEN-01)</name>
  <files>daemon/claude_usage_daemon_windows.py, daemon/tests/test_windows_token.py</files>
  <behavior>
    Cases (from VALIDATION.md, already encoded as RED tests in plan 01):
    - test_extract_nested_shape: _extract_access_token(nested blob) -> "sk-ant-test-1234"
    - test_extract_direct_shape: _extract_access_token(direct blob) -> "sk-ant-test-5678"
    - test_read_token_env_override: CLAUDE_CREDENTIALS_PATH set -> token from that file
    - test_read_token_primary_path: %USERPROFILE%\.claude\.credentials.json present -> its token
    - test_read_token_localappdata_fallback: primary absent, LOCALAPPDATA present -> fallback-2 token
    - test_read_token_appdata_fallback: only APPDATA present -> fallback-3 token
    - test_read_token_no_file: no candidate exists -> None
  </behavior>
  <implementation>Stdlib-only (datetime, json, os, re, sys, pathlib). Copy _extract_access_token verbatim from macOS daemon §57-86. read_token iterates _windows_credential_candidates() first-hit-wins, catching OSError. __main__ prints redacted token + expiry or actionable failure + exit 1.</implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1 (RED to GREEN): Implement _extract_access_token, candidates, and read_token</name>
  <read_first>
    - daemon/claude_usage_daemon.py §57-86 (_extract_access_token — the VERBATIM copy source per D-08; do NOT import this module)
    - daemon/claude_usage_daemon.py §115-127 (read_token / _read_token_file structural reference — the Windows version drops the sys.platform=="darwin" Keychain branch per D-04)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 41-138 — exact import block, verbatim function, _windows_credential_candidates, and read_token bodies)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 lines 141-174 candidate priority; anti-patterns lines 214-220 — no expandvars, no \\wsl$, no platform branch)
    - daemon/tests/test_windows_token.py (the RED suite this must satisfy — created in plan 01)
  </read_first>
  <files>daemon/claude_usage_daemon_windows.py</files>
  <behavior>
    - _extract_access_token: nested claudeAiOauth -> token; direct accessToken -> token; regex fallback; raw-token fullmatch; empty/garbage -> None
    - _windows_credential_candidates: returns [Path(CLAUDE_CREDENTIALS_PATH)] if set; else [Path(CLAUDE_CONFIG_DIR)/".credentials.json"] if set; else the 3 D-02 candidates in order (home/.claude, LOCALAPPDATA/Claude, APPDATA/Claude)
    - read_token: iterate candidates, return first _extract_access_token(path.read_text()) for a path that opens; OSError (incl. PermissionError) -> try next; exhausted -> None
  </behavior>
  <action>
    RED: confirm `python -m pytest daemon/tests/test_windows_token.py -x -q` fails at import (module absent) — the inherited RED state from plan 01. This is the starting gate.
    GREEN: create `daemon/claude_usage_daemon_windows.py` with shebang `#!/usr/bin/env python3` and a module docstring naming Phase 1 plus the BLE/API-deferred note. Imports: `datetime, json, os, re, sys` and `from pathlib import Path` (stdlib only — NO asyncio/bleak/httpx). (1) Copy `_extract_access_token` byte-for-byte from `daemon/claude_usage_daemon.py` §57-86 — same docstring, same branches (direct `accessToken`, nested-dict loop over `data.values()`, regex `"accessToken"\s*:\s*"([^"]+)"`, raw-token `re.fullmatch` with `[A-Za-z0-9_\-.~+/=]{20,}`). (2) Define `_windows_credential_candidates() -> list[Path]`: if `os.environ.get("CLAUDE_CREDENTIALS_PATH")` truthy return `[Path(that)]` (D-03 wins first); elif `os.environ.get("CLAUDE_CONFIG_DIR")` truthy return `[Path(that)/".credentials.json"]`; else return the three D-02 candidates using `Path.home()` (NOT `os.path.expandvars`), `Path(os.environ.get("LOCALAPPDATA", home/"AppData"/"Local"))`, `Path(os.environ.get("APPDATA", home/"AppData"/"Roaming"))` — order: `home/".claude"/".credentials.json"`, `local_appdata/"Claude"/".credentials.json"`, `appdata/"Claude"/".credentials.json"`. (3) Define `read_token() -> str | None`: loop over `_windows_credential_candidates()`, `try` return `_extract_access_token(path.read_text(encoding="utf-8"))`, `except OSError` continue; return `None` after the loop. Run the suite until all 7 tests are GREEN. Commit `feat(01-02): implement Windows token reader (_extract_access_token, read_token)`.
  </action>
  <verify>
    <automated>python -m pytest daemon/tests/test_windows_token.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/claude_usage_daemon_windows.py` contains `def _extract_access_token(`, `def _windows_credential_candidates(`, and `def read_token(`
    - `_extract_access_token` body is byte-identical to `daemon/claude_usage_daemon.py` §57-86 (same branches, same regexes)
    - `grep -E "bleak|httpx|asyncio|expandvars|wsl" daemon/claude_usage_daemon_windows.py` returns nothing (no forbidden imports/patterns)
    - `python -m pytest daemon/tests/test_windows_token.py -x -q` exits 0 (all 7 GREEN)
    - No `sys.platform == "darwin"` Keychain branch (D-04: file-search only)
  </acceptance_criteria>
  <done>All 7 TOKEN-01 tests pass; token reading works across env overrides and the 3-candidate fallback; the copied extractor matches the macOS source exactly.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN to REFACTOR): Add expiry helper and the redacted __main__</name>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 142-196 — __main__ pattern and _read_expiry helper)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 lines 176-197 millisecond decode; Pattern 3 lines 199-212 redaction; Pitfall 1 WSL warning lines 289-297; Pitfall 2 ms-not-seconds lines 299-307)
    - daemon/claude_usage_daemon_windows.py (the file from Task 1 — extend it, do not rewrite the proven functions)
  </read_first>
  <files>daemon/claude_usage_daemon_windows.py</files>
  <behavior>
    - _read_expiry: reads first-hit candidate, pulls claudeAiOauth.expiresAt (epoch MS), returns "YYYY-MM-DD HH:MM UTC" via fromtimestamp(ms/1000, tz=utc); missing/garbage -> "expiry unknown"
    - __main__ success: prints `Token OK (sk-ant-…<last4>), expires <date>` — last 4 chars only, never the full token
    - __main__ failure: prints "No Windows token found — install Claude Code natively on Windows and run 'claude login'." then sys.exit(1)
    - __main__ under non-win32: prints a stderr warning that WSL/Linux paths will not resolve (Pitfall 1) but still proceeds
  </behavior>
  <action>
    Add `_read_expiry() -> str`: loop `_windows_credential_candidates()`, read the first that opens, `json.loads`, `data.get("claudeAiOauth", {}).get("expiresAt")`; if None return `"expiry unknown"`; else `datetime.datetime.fromtimestamp(expires_ms / 1000, tz=datetime.timezone.utc).strftime("%Y-%m-%d %H:%M UTC")`; wrap parsing in `except (TypeError, ValueError, OSError, json.JSONDecodeError)` returning `"expiry unknown"`. CRITICAL: divide expiresAt by 1000 — it is JS-convention milliseconds; the raw value yields year ~57000 (RESEARCH Pitfall 2). Add the `if __name__ == "__main__":` block: if `sys.platform != "win32"` print the WSL warning to `sys.stderr`; call `read_token()`; if `None` print the D-07 actionable message exactly `No Windows token found — install Claude Code natively on Windows and run 'claude login'.` and `sys.exit(1)`; else compute `_read_expiry()` and `print(f"Token OK (sk-ant-…{token[-4:]}), expires {expiry}")`. NEVER print the full `token` variable anywhere (D-06). REFACTOR: ensure `_read_expiry` and `read_token` both call the shared `_windows_credential_candidates()` (no duplicated path logic); keep the module minimal (D-05) — no logging framework, no extra config-dir helper, no main loop. Re-run the suite to confirm the 7 tests still pass. Commit `feat(01-02): redacted token+expiry output for __main__`.
  </action>
  <verify>
    <automated>python -m pytest daemon/tests/test_windows_token.py -q && CLAUDE_CREDENTIALS_PATH=daemon/tests/fixtures/credentials_nested.json python daemon/claude_usage_daemon_windows.py 2>/dev/null | grep -qE "^Token OK \(sk-ant-…1234\), expires 2[0-9]{3}-" && echo OUTPUT_OK</automated>
    <automated>CLAUDE_CREDENTIALS_PATH=/nonexistent/x.json python daemon/claude_usage_daemon_windows.py >/dev/null 2>&1; test $? -eq 1 && echo EXIT1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/claude_usage_daemon_windows.py` contains `def _read_expiry(` and an `if __name__ == "__main__":` block
    - Running with `CLAUDE_CREDENTIALS_PATH` pointed at `credentials_nested.json` prints a line matching `^Token OK \(sk-ant-…1234\), expires 2[0-9]{3}-` (redacted last-4 `1234`, real year, NOT year ~57000)
    - The full token string never appears in stdout (only `sk-ant-…1234`)
    - Running with a nonexistent `CLAUDE_CREDENTIALS_PATH` prints the actionable message and exits with code 1
    - `python -m pytest daemon/tests/test_windows_token.py -q` still exits 0 (no regression)
    - `grep -c "expires_ms / 1000" daemon/claude_usage_daemon_windows.py` is at least 1 (ms-to-s conversion present)
  </acceptance_criteria>
  <done>The scaffold reads a real token and prints a redacted confirmation + correctly-decoded expiry; failure path is actionable and exits non-zero; no full token ever printed. Vertical slice complete end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → daemon process | `.credentials.json` is untrusted input parsed with json.loads / regex inside _extract_access_token |
| env vars → path resolution | CLAUDE_CREDENTIALS_PATH / CLAUDE_CONFIG_DIR are user-controlled overrides |
| daemon process → terminal/scrollback | Token output crosses into shell history — the dominant Info-Disclosure surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-ID | Information Disclosure | __main__ token output | mitigate | D-06: print only `sk-ant-…{token[-4:]}`; NEVER `print(token)`. Acceptance criteria assert the full token never appears in stdout. (ASVS V2 — token-handling.) |
| T-01-IV | Tampering / Input Validation | _extract_access_token JSON parse | mitigate | stdlib `json.loads` (no eval); regex fallback is read-only; raw-token branch validated with `re.fullmatch` charset. (ASVS V5 — input validation.) |
| T-01-PT | Tampering | path traversal via CLAUDE_CREDENTIALS_PATH/CLAUDE_CONFIG_DIR | accept | The override is the user's own env var pointing at their own filesystem; no privilege boundary crossed. Low risk per RESEARCH Security Domain. |
| T-01-WSL | Information Disclosure / wrong-path | running under WSL Python | mitigate | Pitfall 1: __main__ prints a stderr warning when `sys.platform != "win32"`; D-07 failure message guides the user. No `\\wsl$`/`wsl.exe` path is ever constructed (Path.home() only). |
| T-01-SC | Tampering | pip installs | accept | No package-manager installs — Phase 1 is stdlib-only (RESEARCH Package Legitimacy Audit: zero external packages). pytest 8.4.2 already present. |
</threat_model>

<verification>
- `python -m pytest daemon/tests/test_windows_token.py -q` exits 0 (all 7 TOKEN-01 cases GREEN).
- Manual run against the nested fixture prints `Token OK (sk-ant-…1234), expires <real-year>` and never the full token.
- Failure run exits 1 with the actionable message.

## Manual-Only Verification (Success Criterion #3 — cannot be CI-automated)
On a real native-Windows machine with Claude Code installed and `claude login` completed:
run `python daemon\claude_usage_daemon_windows.py` and confirm it prints a redacted token + expiry sourced from `%USERPROFILE%\.claude\.credentials.json`, touching no `\\wsl$` path. This is the single manual verification recorded in VALIDATION.md; it is NOT automatable in WSL/CI because it requires native Windows path resolution and a live token.
</verification>

<success_criteria>
- TOKEN-01 satisfied: all 7 unit cases pass.
- Success Criterion #2 satisfied: `read_token()` reads the OAuth token from the native-Windows path and returns the access-token string.
- Success Criterion #3 ready for manual confirmation: `__main__` prints expiry/redacted token without touching WSL paths.
- Minimal scaffold (D-05) respected: stdlib-only, no BLE/API/logging/main-loop.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
