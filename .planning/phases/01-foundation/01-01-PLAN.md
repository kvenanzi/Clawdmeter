---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/windows-daemon-port.md
  - daemon/__init__.py
  - daemon/tests/__init__.py
  - daemon/tests/fixtures/credentials_nested.json
  - daemon/tests/fixtures/credentials_direct.json
  - daemon/tests/test_windows_token.py
  - conftest.py
autonomous: true
requirements: [TOKEN-01]

must_haves:
  truths:
    - "The GATT-encryption verdict (UNENCRYPTED) is recorded in .planning/notes/windows-daemon-port.md (Success Criterion #1)"
    - "pytest is runnable and daemon/tests/ is an importable test package"
    - "A failing (RED) test suite exists covering all 7 TOKEN-01 cases — it imports the not-yet-written Windows daemon module"
  artifacts:
    - path: ".planning/notes/windows-daemon-port.md"
      provides: "GATT encryption verdict section (D-01)"
      contains: "UNENCRYPTED"
    - path: "daemon/tests/test_windows_token.py"
      provides: "7 TOKEN-01 unit tests (RED until plan 02)"
      contains: "def test_read_token_primary_path"
    - path: "daemon/tests/fixtures/credentials_nested.json"
      provides: "nested claudeAiOauth fixture"
      contains: "claudeAiOauth"
    - path: "daemon/tests/fixtures/credentials_direct.json"
      provides: "direct accessToken fixture"
      contains: "accessToken"
    - path: "conftest.py"
      provides: "repo root on sys.path so `import daemon.*` resolves"
  key_links:
    - from: "daemon/tests/test_windows_token.py"
      to: "daemon.claude_usage_daemon_windows"
      via: "from daemon.claude_usage_daemon_windows import _extract_access_token, read_token"
      pattern: "from daemon.claude_usage_daemon_windows import"
---

<objective>
Record the GATT-encryption verdict (D-01) and stand up the pytest test infrastructure with a complete RED test suite for the Windows token reader.

This is Wave 0 of a two-plan, single-vertical-slice phase. The vertical slice is "read a real Windows token end-to-end and print a redacted confirmation." This plan delivers the failing end-to-end test (RED) and the de-risk note; plan 02 turns the suite GREEN by implementing the reader.

Purpose: Satisfy Success Criterion #1 (GATT verdict written to the note) and close the "what if it's encrypted?" contingency forever. Establish the pytest convention that does not yet exist in the repo, so plan 02's TDD cycle has a test harness to drive against.
Output: Updated note with the D-01 verdict; `daemon/tests/` package with fixtures, a root `conftest.py`, and `test_windows_token.py` holding all 7 TOKEN-01 cases (RED — module under test does not exist yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- The module under test (created in plan 02). The test suite must import these exact names. -->
<!-- daemon/claude_usage_daemon_windows.py will export: -->
<!--   _extract_access_token(blob: str) -> str | None   (verbatim copy from macOS daemon §57-86) -->
<!--   read_token() -> str | None                        (file-search, first-hit-wins, env overrides) -->
<!--   _windows_credential_candidates() -> list[Path]    (env overrides + 3 D-02 candidates) -->
<!-- Import line the tests MUST use (per PATTERNS.md): -->
<!--   from daemon.claude_usage_daemon_windows import _extract_access_token, read_token, _windows_credential_candidates -->
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Record the GATT-encryption verdict in the note</name>
  <read_first>
    - .planning/notes/windows-daemon-port.md (current file ends at line 72; "Encryption gate" open question is in the BLE research findings #2 and Open questions list)
    - .planning/phases/01-foundation/01-PATTERNS.md (the exact append block is specified there under the `.planning/notes/windows-daemon-port.md` section, lines 309-341)
    - firmware/src/ble.cpp §183-202 (the characteristic flags that prove the verdict — already confirmed: RX=WRITE|WRITE_NR, TX=READ|NOTIFY, REQ=NOTIFY, no _ENC/_AUTHEN/_AUTHOR)
  </read_first>
  <files>.planning/notes/windows-daemon-port.md</files>
  <action>
    Append a new H2 section `## GATT encryption gate — verdict (D-01)` to the END of the note (after the existing "Open questions / next steps" section). Match the file's existing heading style (H2 `##`, plain prose, bullets, no new front-matter). The section MUST state the verdict literally as UNENCRYPTED and record the three characteristics with their plain NimBLE flags: RX (`…0002`) = `NIMBLE_PROPERTY::WRITE | WRITE_NR`, TX (`…0003`) = `NIMBLE_PROPERTY::READ | NOTIFY`, REQ (`…0004`) = `NIMBLE_PROPERTY::NOTIFY`. Note that NimBLE's `_ENC`/`_AUTHEN` variants exist but are used only on the HID keyboard, not the custom data service. State the consequence: no manual Bluetooth pairing and no firmware change needed. Close with a line marking it satisfies Phase 1 Success Criterion #1. Use the exact wording from 01-PATTERNS.md lines 320-340 as the source of truth. Do NOT modify the existing front-matter or earlier sections.
  </action>
  <verify>
    <automated>grep -q "UNENCRYPTED" .planning/notes/windows-daemon-port.md && grep -q "Success Criterion #1" .planning/notes/windows-daemon-port.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/notes/windows-daemon-port.md` contains the string "UNENCRYPTED"
    - `.planning/notes/windows-daemon-port.md` contains a heading "GATT encryption gate — verdict (D-01)"
    - The section names all three characteristics (RX/TX/REQ) with their flags
    - The section contains "Success Criterion #1"
    - The original front-matter block (lines 1-5) and existing sections are unchanged
  </acceptance_criteria>
  <done>The note records the UNENCRYPTED verdict with characteristic flags and Success Criterion #1 reference; Success Criterion #1 is satisfied.</done>
</task>

<task type="execute">
  <name>Task 2: Create pytest fixtures and the daemon test package</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements, lines 56-62 — exact fixture contents)
    - .planning/phases/01-foundation/01-PATTERNS.md (fixtures section lines 280-305 — exact JSON, and the no-analog note about pytest infra)
    - daemon/claude_usage_daemon.py §1-7 (shebang + module docstring convention to mirror in the test file)
    - daemon/test_macos_connect.py (existing style reference; it is NOT pytest — do not mirror its structure, only its docstring style)
  </read_first>
  <files>daemon/__init__.py, daemon/tests/__init__.py, daemon/tests/fixtures/credentials_nested.json, daemon/tests/fixtures/credentials_direct.json, conftest.py</files>
  <action>
    Create the test package skeleton so `from daemon.claude_usage_daemon_windows import ...` resolves when running `python -m pytest` from the repo root. (1) Create empty `daemon/__init__.py` and empty `daemon/tests/__init__.py` (the macOS daemon's heavy top-level imports `bleak`/`httpx` are NOT triggered — `daemon/__init__.py` stays empty, importing a submodule does not run sibling modules). (2) Create `conftest.py` at the repo root that prepends the repo root directory to `sys.path` (use `sys.path.insert(0, os.path.dirname(__file__))`) so the `daemon` package is importable regardless of pytest's rootdir behavior. (3) Create `daemon/tests/fixtures/credentials_nested.json` with the nested shape: a `claudeAiOauth` object containing `accessToken` = `sk-ant-test-1234`, `refreshToken` = `sk-ant-ort-test-5678`, `expiresAt` = `9999999999000` (far-future ms epoch), and `scopes` = `["user:inference", "user:profile"]`. (4) Create `daemon/tests/fixtures/credentials_direct.json` with the direct shape: `accessToken` = `sk-ant-test-5678`. Use the exact JSON from 01-PATTERNS.md lines 286-303.
  </action>
  <verify>
    <automated>python -m pytest --version && python -c "import json; assert json.load(open('daemon/tests/fixtures/credentials_nested.json'))['claudeAiOauth']['accessToken']=='sk-ant-test-1234'; assert json.load(open('daemon/tests/fixtures/credentials_direct.json'))['accessToken']=='sk-ant-test-5678'; print('fixtures OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/__init__.py` and `daemon/tests/__init__.py` exist (empty)
    - `conftest.py` at repo root contains `sys.path.insert`
    - `daemon/tests/fixtures/credentials_nested.json` parses and `claudeAiOauth.accessToken` == `sk-ant-test-1234` and `expiresAt` == `9999999999000`
    - `daemon/tests/fixtures/credentials_direct.json` parses and `accessToken` == `sk-ant-test-5678`
    - `python -m pytest --version` exits 0
  </acceptance_criteria>
  <done>Test package, fixtures, and sys.path conftest exist; pytest runs and fixtures parse with the expected token values.</done>
</task>

<task type="execute">
  <name>Task 3: Write the RED TOKEN-01 test suite (all 7 cases)</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map lines 44-50 — the 7 named test functions and their exact pytest node IDs)
    - .planning/phases/01-foundation/01-PATTERNS.md (test file section lines 229-276 — conventions: import line, tmp_path + monkeypatch usage, fixtures-by-path)
    - .planning/phases/01-foundation/01-RESEARCH.md (Phase Requirements → Test Map lines 449-457 — behavior each test asserts; candidate path priority D-02; env override D-03)
    - daemon/tests/fixtures/credentials_nested.json and credentials_direct.json (created in Task 2 — the values to assert against)
  </read_first>
  <files>daemon/tests/test_windows_token.py</files>
  <action>
    Write a pytest module with a module docstring naming TOKEN-01 and the run command `python -m pytest daemon/tests/test_windows_token.py -x -q`. Import the (not-yet-existing) module with exactly: `from daemon.claude_usage_daemon_windows import _extract_access_token, read_token, _windows_credential_candidates`. Define `FIXTURES = Path(__file__).parent / "fixtures"`. Write these 7 test functions with EXACTLY these names (they must match the VALIDATION.md node IDs):
      1. `test_extract_nested_shape` — reads credentials_nested.json, asserts `_extract_access_token(blob) == "sk-ant-test-1234"`.
      2. `test_extract_direct_shape` — reads credentials_direct.json, asserts `_extract_access_token(blob) == "sk-ant-test-5678"`.
      3. `test_read_token_env_override` — writes a creds file in `tmp_path`, sets `CLAUDE_CREDENTIALS_PATH` via `monkeypatch.setenv` to that file, asserts `read_token()` returns its token. Also `monkeypatch.delenv("CLAUDE_CONFIG_DIR", raising=False)` to isolate.
      4. `test_read_token_primary_path` — creates `tmp_path/".claude"/".credentials.json"` (nested shape), monkeypatches `_windows_credential_candidates` (or `Path.home`) so the primary candidate resolves to it; delenv both `CLAUDE_CREDENTIALS_PATH` and `CLAUDE_CONFIG_DIR`; asserts the token comes back.
      5. `test_read_token_localappdata_fallback` — primary path absent, second candidate (`%LOCALAPPDATA%\Claude\.credentials.json`) present; assert `read_token()` returns the fallback-2 token. Use monkeypatch on `_windows_credential_candidates` to return `[missing_primary, present_localappdata, missing_appdata]`.
      6. `test_read_token_appdata_fallback` — only the third candidate (`%APPDATA%\Claude\.credentials.json`) present; assert `read_token()` returns the fallback-3 token.
      7. `test_read_token_no_file` — point `CLAUDE_CREDENTIALS_PATH` at a nonexistent path in `tmp_path` (and delenv `CLAUDE_CONFIG_DIR`); assert `read_token()` is `None`.
    For the fallback tests (5, 6), monkeypatch the module's `_windows_credential_candidates` function to return a controlled list of `Path` objects, so the first-hit-wins loop is exercised deterministically without depending on real env vars. Use the fixture token values where reading fixtures; use distinct sentinel tokens (e.g. `sk-ant-test-LA`, `sk-ant-test-APP`) for the fallback files so the assertions prove WHICH candidate won.
    Do NOT implement the daemon module here — that is plan 02. This suite MUST currently fail at import (RED): the module does not exist yet.
  </action>
  <verify>
    <automated>python -m pytest daemon/tests/test_windows_token.py --co -q 2>&1 | grep -qiE "ModuleNotFoundError|No module named|error" && echo "RED-as-expected (module not yet implemented)"</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/tests/test_windows_token.py` contains all 7 function names: `test_extract_nested_shape`, `test_extract_direct_shape`, `test_read_token_env_override`, `test_read_token_primary_path`, `test_read_token_localappdata_fallback`, `test_read_token_appdata_fallback`, `test_read_token_no_file`
    - The import line is `from daemon.claude_usage_daemon_windows import _extract_access_token, read_token, _windows_credential_candidates`
    - Collection currently fails because `daemon.claude_usage_daemon_windows` does not exist (RED state — expected; plan 02 turns it GREEN)
    - No production code for the daemon module is written in this plan
  </acceptance_criteria>
  <done>All 7 TOKEN-01 tests exist with the exact VALIDATION.md names and import the future module; the suite is RED (module absent), establishing the failing end-to-end harness for plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → daemon process | The credentials file (and test fixtures) are untrusted input parsed as JSON |
| BLE link (firmware ↔ host) | Verified UNENCRYPTED for the custom data service — recorded, not exercised this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | test fixtures / note | mitigate | Fixtures use obviously-fake `sk-ant-test-*` tokens; no real credential ever committed. Note records flags only, no secrets. |
| T-01-02 | Tampering | GATT verdict accuracy | accept | Verdict is read-only firmware inspection; flags are compile-time constants in ble.cpp §185-199. Low risk of drift within phase. |
| T-01-SC | Tampering | pip installs | accept | No package-manager installs in this plan — pytest 8.4.2 already present (verified). Phase 1 is stdlib-only per RESEARCH Package Legitimacy Audit (zero external packages). |
</threat_model>

<verification>
- `python -m pytest --version` exits 0 (pytest infra live).
- `.planning/notes/windows-daemon-port.md` contains "UNENCRYPTED" and "Success Criterion #1".
- `daemon/tests/test_windows_token.py` collection is RED (module under test absent) — expected handoff state to plan 02.
- Both fixture JSONs parse with expected token values.
</verification>

<success_criteria>
- Success Criterion #1 satisfied: GATT verdict recorded in the note.
- pytest runnable; `daemon/tests/` is an importable package via root `conftest.py`.
- 7 named TOKEN-01 tests exist (RED), forming the failing end-to-end harness for the vertical slice.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
